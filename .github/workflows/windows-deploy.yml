name: Deploy for Windows

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  version-check:
    uses: ./.github/workflows/version-checks.yml

  build:
    runs-on: windows-latest
    needs: version-check
    if: needs.version-check.outputs.should-deploy == 'true'

    steps:
    - uses: actions/checkout@v5
    
    - name: Setup .NET 10.0
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: 10.0.x
    
    - name: Install .NET MAUI Workload
      run: dotnet workload install maui --skip-sign-check
    
    - name: Create Secrets.local_secrets.cs
      run: |
        $secretsContent = @"
        namespace SVFDriveLibrary.DataAccess;

        public static partial class Secrets
        {
            static Secrets()
            {
                AzureConnectionString = "${{ secrets.AzureConnectionString }}";

                SyncfusionLicense = "${{ secrets.SyncfusionLicense }}";

                EmailPassword = "${{ secrets.EmailPassword }}";
                ToEmail = "${{ secrets.ToEmail }}";

                RelayUrl = "${{ secrets.RelayUrl }}";
            }
        }
        "@
        Set-Content -Path "SVFDriveLibrary/DataAccess/Secrets.local_secrets.cs" -Value $secretsContent
    
    - name: Restore dependencies
      run: dotnet restore SVFDrive/SVFDrive/SVFDrive.csproj
    
    - name: Publish Windows MAUI App
      run: dotnet publish SVFDrive/SVFDrive/SVFDrive.csproj -f net10.0-windows10.0.19041.0 -c Release -p:WindowsPackageType=None -o SVFDrive/SVFDrive/publish/windows
    
    - name: Create Zip Archive
      run: Compress-Archive -Path "SVFDrive/SVFDrive/publish/windows/*" -DestinationPath "SVFDrive.zip" -Force
    
    - name: Upload Windows Build Artifact
      uses: actions/upload-artifact@v4
      with:
        name: windows-release
        path: SVFDrive.zip

  deploy:
    runs-on: windows-latest
    needs: [version-check, build]
    if: needs.version-check.outputs.should-deploy == 'true'
    permissions:
      contents: write

    steps:
    - name: Download artifact from build job
      uses: actions/download-artifact@v4
      with:
        name: windows-release
    
    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ needs.version-check.outputs.current-version }}
        name: ${{ github.event.head_commit.message }}
        files: SVFDrive.zip
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Deployment Summary
      run: |
        echo "🚀 Successfully deployed SVFDrive Windows version ${{ needs.version-check.outputs.current-version }} to GitHub Releases"
