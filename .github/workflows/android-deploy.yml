name: Deploy SVFDrive for Android

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  version-check:
    uses: ./.github/workflows/version-checks.yml

  build:
    runs-on: windows-latest
    needs: version-check
    if: needs.version-check.outputs.should-deploy == 'true'

    steps:
    - uses: actions/checkout@v5
    
    - name: Setup .NET 10.0
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: 10.0.x
    
    - name: Install .NET MAUI Workload
      run: dotnet workload install maui --skip-sign-check
    
    - name: Create Secrets.local_secrets.cs
      run: |
        $secretsContent = @"
        namespace SVFDriveLibrary.DataAccess;

        public static partial class Secrets
        {
            static Secrets()
            {
                AzureConnectionString = "${{ secrets.AzureConnectionString }}";

                SyncfusionLicense = "${{ secrets.SyncfusionLicense }}";

                EmailPassword = "${{ secrets.EmailPassword }}";
                ToEmail = "${{ secrets.ToEmail }}";

                RelayUrl = "${{ secrets.RelayUrl }}";
            }
        }
        "@
        Set-Content -Path "SVFDriveLibrary/DataAccess/Secrets.local_secrets.cs" -Value $secretsContent
    
    - name: Setup Keystore
      run: |
        # Decode base64 keystore and save it
        $keystoreBase64 = "${{ secrets.ANDROID_KEYSTORE_BASE64 }}"
        $keystoreBytes = [System.Convert]::FromBase64String($keystoreBase64)
        $keystorePath = "SVFDrive/SVFDrive/svfdrive.keystore"
        [System.IO.File]::WriteAllBytes($keystorePath, $keystoreBytes)
        echo "Keystore created at: $keystorePath"
    
    - name: Restore dependencies
      run: dotnet restore SVFDrive/SVFDrive/SVFDrive.csproj
    
    - name: Build Signed Android APK
      run: |
        dotnet publish SVFDrive/SVFDrive/SVFDrive.csproj `
          -f net10.0-android `
          -c Release `
          -p:AndroidPackageFormat=apk `
          -p:AndroidKeyStore=true `
          -p:AndroidSigningKeyStore=svfdrive.keystore `
          -p:AndroidSigningKeyAlias=SVFDrive `
          -p:AndroidSigningKeyPass="${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" `
          -p:AndroidSigningStorePass="${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" `
          -o SVFDrive/SVFDrive/publish/android
    
    - name: Find and Rename APK
      id: find-apk
      run: |
        $apkPath = Get-ChildItem -Path "SVFDrive/SVFDrive/publish/android" -Filter "*.apk" -Recurse | Select-Object -First 1 -ExpandProperty FullName
        echo "APK found at: $apkPath"
        
        # Rename to match update artifact naming.
        Copy-Item $apkPath -Destination "SVFDrive.apk"
        echo "APK renamed to: SVFDrive.apk"
    
    - name: Upload Android Build Artifact
      uses: actions/upload-artifact@v4
      with:
        name: android-release
        path: SVFDrive.apk

  deploy:
    runs-on: windows-latest
    needs: [version-check, build]
    if: needs.version-check.outputs.should-deploy == 'true'
    permissions:
      contents: write

    steps:
    - name: Download artifact from build job
      uses: actions/download-artifact@v4
      with:
        name: android-release
    
    - name: Upload APK to Existing Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ needs.version-check.outputs.current-version }}
        files: SVFDrive.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Deployment Summary
      run: |
        echo "🚀 Successfully deployed SVFDrive Android APK to release ${{ needs.version-check.outputs.current-version }}"
